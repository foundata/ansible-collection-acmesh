---

trivial:
  - >
    molecule test code - The converge fixtures now plant a canary credential in
    a certificate's ``environment`` mapping, and the verify stage prints how to
    check a captured Molecule log for it. Callbacks print on the controller, so
    this cannot be asserted from within the scenario; the check is meaningful
    for credential-bearing runs captured at ``-vvv`` with forced diff.
